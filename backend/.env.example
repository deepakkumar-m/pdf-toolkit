# Environment Configuration
NODE_ENV=production
PORT=3001

# Frontend URL (set this to your Netlify URL in production)
FRONTEND_URL=http://localhost:3000

# Ghostscript binary path (leave empty to use system 'gs')
GS_EXEC=
